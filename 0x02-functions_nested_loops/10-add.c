#include "main.h"

/**
 * add - Entry point for the addition
 * @k: first value for adding
 * @m: second value for adding
 * Return: Always 0 (success)
 */

int add(int k, int m)

{
	return (k + m);
}
